// <auto-generated>
//     Generated by the protocol buffer compiler.  DO NOT EDIT!
//     source: authzed/api/materialize/v0/roaringlookupresources.proto
// </auto-generated>
#pragma warning disable 0414, 1591, 8981, 0612
#region Designer generated code

using grpc = global::Grpc.Core;

namespace Authzed.Api.Materialize.V0 {
  public static partial class RoaringLookupResourcesService
  {
    static readonly string __ServiceName = "authzed.api.materialize.v0.RoaringLookupResourcesService";

    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    static void __Helper_SerializeMessage(global::Google.Protobuf.IMessage message, grpc::SerializationContext context)
    {
      #if !GRPC_DISABLE_PROTOBUF_BUFFER_SERIALIZATION
      if (message is global::Google.Protobuf.IBufferMessage)
      {
        context.SetPayloadLength(message.CalculateSize());
        global::Google.Protobuf.MessageExtensions.WriteTo(message, context.GetBufferWriter());
        context.Complete();
        return;
      }
      #endif
      context.Complete(global::Google.Protobuf.MessageExtensions.ToByteArray(message));
    }

    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    static class __Helper_MessageCache<T>
    {
      public static readonly bool IsBufferMessage = global::System.Reflection.IntrospectionExtensions.GetTypeInfo(typeof(global::Google.Protobuf.IBufferMessage)).IsAssignableFrom(typeof(T));
    }

    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    static T __Helper_DeserializeMessage<T>(grpc::DeserializationContext context, global::Google.Protobuf.MessageParser<T> parser) where T : global::Google.Protobuf.IMessage<T>
    {
      #if !GRPC_DISABLE_PROTOBUF_BUFFER_SERIALIZATION
      if (__Helper_MessageCache<T>.IsBufferMessage)
      {
        return parser.ParseFrom(context.PayloadAsReadOnlySequence());
      }
      #endif
      return parser.ParseFrom(context.PayloadAsNewBuffer());
    }

    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    static readonly grpc::Marshaller<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest> __Marshaller_authzed_api_materialize_v0_ExperimentalRoaringLookupResourcesRequest = grpc::Marshallers.Create(__Helper_SerializeMessage, context => __Helper_DeserializeMessage(context, global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest.Parser));
    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    static readonly grpc::Marshaller<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse> __Marshaller_authzed_api_materialize_v0_ExperimentalRoaringLookupResourcesResponse = grpc::Marshallers.Create(__Helper_SerializeMessage, context => __Helper_DeserializeMessage(context, global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse.Parser));

    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    static readonly grpc::Method<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest, global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse> __Method_ExperimentalRoaringLookupResources = new grpc::Method<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest, global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse>(
        grpc::MethodType.Unary,
        __ServiceName,
        "ExperimentalRoaringLookupResources",
        __Marshaller_authzed_api_materialize_v0_ExperimentalRoaringLookupResourcesRequest,
        __Marshaller_authzed_api_materialize_v0_ExperimentalRoaringLookupResourcesResponse);

    /// <summary>Service descriptor</summary>
    public static global::Google.Protobuf.Reflection.ServiceDescriptor Descriptor
    {
      get { return global::Authzed.Api.Materialize.V0.RoaringlookupresourcesReflection.Descriptor.Services[0]; }
    }

    /// <summary>Base class for server-side implementations of RoaringLookupResourcesService</summary>
    [grpc::BindServiceMethod(typeof(RoaringLookupResourcesService), "BindService")]
    public abstract partial class RoaringLookupResourcesServiceBase
    {
      /// <summary>
      /// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
      /// of the resources of the given type on which the given subject has the
      /// given permission. This API is experimental and subject to change or
      /// removal.
      ///
      /// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
      /// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
      /// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
      /// query against a `long` field, once Base64-encoded.
      ///
      /// The IDs in the bitmap are the resource object IDs exactly as they appear
      /// in the relationships: no surrogate or internal ID is introduced, so the
      /// caller can use the bitmap directly against its own data (for example, a
      /// search index keyed by the same IDs).
      ///
      /// For this API to be usable, every resource object ID of the requested type
      /// must be a canonical decimal integer that fits in 44 bits -- at most
      /// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
      /// uint64 formatting unchanged: `document:007` and `document:7` are distinct
      /// objects that would collide as the integer 7, so non-canonical IDs are
      /// rejected. If any resource object ID violates either rule, the call fails
      /// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
      /// bitmap that is quietly too small is a wrong authorization answer. When the
      /// permission relates a type to itself (for example `group#member` looked up
      /// for a `group#member` subject), the subject is one of its own resources, so
      /// the subject's object ID is held to the same rules and can itself be the ID
      /// named in that error.
      ///
      /// Response size: the whole bitmap is returned in a single unary message, and
      /// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
      /// is compact -- a million sequential IDs encode in a few hundred bytes -- but
      /// sparse IDs cost close to 10 bytes each, so a result of more than roughly
      /// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
      /// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
      /// a limit of the caller's own gRPC configuration, not of the service: raise
      /// it to match the largest result you expect (in Go,
      /// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
      /// have an equivalent channel option). The `cardinality` field is returned so
      /// callers can see how large a result is once received; no server-side result
      /// limit is applied.
      /// </summary>
      /// <param name="request">The request received from the client.</param>
      /// <param name="context">The context of the server-side call handler being invoked.</param>
      /// <returns>The response to send back to the client (wrapped by a task).</returns>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public virtual global::System.Threading.Tasks.Task<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse> ExperimentalRoaringLookupResources(global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest request, grpc::ServerCallContext context)
      {
        throw new grpc::RpcException(new grpc::Status(grpc::StatusCode.Unimplemented, ""));
      }

    }

    /// <summary>Client for RoaringLookupResourcesService</summary>
    public partial class RoaringLookupResourcesServiceClient : grpc::ClientBase<RoaringLookupResourcesServiceClient>
    {
      /// <summary>Creates a new client for RoaringLookupResourcesService</summary>
      /// <param name="channel">The channel to use to make remote calls.</param>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public RoaringLookupResourcesServiceClient(grpc::ChannelBase channel) : base(channel)
      {
      }
      /// <summary>Creates a new client for RoaringLookupResourcesService that uses a custom <c>CallInvoker</c>.</summary>
      /// <param name="callInvoker">The callInvoker to use to make remote calls.</param>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public RoaringLookupResourcesServiceClient(grpc::CallInvoker callInvoker) : base(callInvoker)
      {
      }
      /// <summary>Protected parameterless constructor to allow creation of test doubles.</summary>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      protected RoaringLookupResourcesServiceClient() : base()
      {
      }
      /// <summary>Protected constructor to allow creation of configured clients.</summary>
      /// <param name="configuration">The client configuration.</param>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      protected RoaringLookupResourcesServiceClient(ClientBaseConfiguration configuration) : base(configuration)
      {
      }

      /// <summary>
      /// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
      /// of the resources of the given type on which the given subject has the
      /// given permission. This API is experimental and subject to change or
      /// removal.
      ///
      /// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
      /// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
      /// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
      /// query against a `long` field, once Base64-encoded.
      ///
      /// The IDs in the bitmap are the resource object IDs exactly as they appear
      /// in the relationships: no surrogate or internal ID is introduced, so the
      /// caller can use the bitmap directly against its own data (for example, a
      /// search index keyed by the same IDs).
      ///
      /// For this API to be usable, every resource object ID of the requested type
      /// must be a canonical decimal integer that fits in 44 bits -- at most
      /// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
      /// uint64 formatting unchanged: `document:007` and `document:7` are distinct
      /// objects that would collide as the integer 7, so non-canonical IDs are
      /// rejected. If any resource object ID violates either rule, the call fails
      /// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
      /// bitmap that is quietly too small is a wrong authorization answer. When the
      /// permission relates a type to itself (for example `group#member` looked up
      /// for a `group#member` subject), the subject is one of its own resources, so
      /// the subject's object ID is held to the same rules and can itself be the ID
      /// named in that error.
      ///
      /// Response size: the whole bitmap is returned in a single unary message, and
      /// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
      /// is compact -- a million sequential IDs encode in a few hundred bytes -- but
      /// sparse IDs cost close to 10 bytes each, so a result of more than roughly
      /// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
      /// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
      /// a limit of the caller's own gRPC configuration, not of the service: raise
      /// it to match the largest result you expect (in Go,
      /// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
      /// have an equivalent channel option). The `cardinality` field is returned so
      /// callers can see how large a result is once received; no server-side result
      /// limit is applied.
      /// </summary>
      /// <param name="request">The request to send to the server.</param>
      /// <param name="headers">The initial metadata to send with the call. This parameter is optional.</param>
      /// <param name="deadline">An optional deadline for the call. The call will be cancelled if deadline is hit.</param>
      /// <param name="cancellationToken">An optional token for canceling the call.</param>
      /// <returns>The response received from the server.</returns>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public virtual global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse ExperimentalRoaringLookupResources(global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest request, grpc::Metadata headers = null, global::System.DateTime? deadline = null, global::System.Threading.CancellationToken cancellationToken = default(global::System.Threading.CancellationToken))
      {
        return ExperimentalRoaringLookupResources(request, new grpc::CallOptions(headers, deadline, cancellationToken));
      }
      /// <summary>
      /// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
      /// of the resources of the given type on which the given subject has the
      /// given permission. This API is experimental and subject to change or
      /// removal.
      ///
      /// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
      /// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
      /// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
      /// query against a `long` field, once Base64-encoded.
      ///
      /// The IDs in the bitmap are the resource object IDs exactly as they appear
      /// in the relationships: no surrogate or internal ID is introduced, so the
      /// caller can use the bitmap directly against its own data (for example, a
      /// search index keyed by the same IDs).
      ///
      /// For this API to be usable, every resource object ID of the requested type
      /// must be a canonical decimal integer that fits in 44 bits -- at most
      /// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
      /// uint64 formatting unchanged: `document:007` and `document:7` are distinct
      /// objects that would collide as the integer 7, so non-canonical IDs are
      /// rejected. If any resource object ID violates either rule, the call fails
      /// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
      /// bitmap that is quietly too small is a wrong authorization answer. When the
      /// permission relates a type to itself (for example `group#member` looked up
      /// for a `group#member` subject), the subject is one of its own resources, so
      /// the subject's object ID is held to the same rules and can itself be the ID
      /// named in that error.
      ///
      /// Response size: the whole bitmap is returned in a single unary message, and
      /// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
      /// is compact -- a million sequential IDs encode in a few hundred bytes -- but
      /// sparse IDs cost close to 10 bytes each, so a result of more than roughly
      /// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
      /// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
      /// a limit of the caller's own gRPC configuration, not of the service: raise
      /// it to match the largest result you expect (in Go,
      /// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
      /// have an equivalent channel option). The `cardinality` field is returned so
      /// callers can see how large a result is once received; no server-side result
      /// limit is applied.
      /// </summary>
      /// <param name="request">The request to send to the server.</param>
      /// <param name="options">The options for the call.</param>
      /// <returns>The response received from the server.</returns>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public virtual global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse ExperimentalRoaringLookupResources(global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest request, grpc::CallOptions options)
      {
        return CallInvoker.BlockingUnaryCall(__Method_ExperimentalRoaringLookupResources, null, options, request);
      }
      /// <summary>
      /// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
      /// of the resources of the given type on which the given subject has the
      /// given permission. This API is experimental and subject to change or
      /// removal.
      ///
      /// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
      /// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
      /// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
      /// query against a `long` field, once Base64-encoded.
      ///
      /// The IDs in the bitmap are the resource object IDs exactly as they appear
      /// in the relationships: no surrogate or internal ID is introduced, so the
      /// caller can use the bitmap directly against its own data (for example, a
      /// search index keyed by the same IDs).
      ///
      /// For this API to be usable, every resource object ID of the requested type
      /// must be a canonical decimal integer that fits in 44 bits -- at most
      /// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
      /// uint64 formatting unchanged: `document:007` and `document:7` are distinct
      /// objects that would collide as the integer 7, so non-canonical IDs are
      /// rejected. If any resource object ID violates either rule, the call fails
      /// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
      /// bitmap that is quietly too small is a wrong authorization answer. When the
      /// permission relates a type to itself (for example `group#member` looked up
      /// for a `group#member` subject), the subject is one of its own resources, so
      /// the subject's object ID is held to the same rules and can itself be the ID
      /// named in that error.
      ///
      /// Response size: the whole bitmap is returned in a single unary message, and
      /// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
      /// is compact -- a million sequential IDs encode in a few hundred bytes -- but
      /// sparse IDs cost close to 10 bytes each, so a result of more than roughly
      /// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
      /// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
      /// a limit of the caller's own gRPC configuration, not of the service: raise
      /// it to match the largest result you expect (in Go,
      /// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
      /// have an equivalent channel option). The `cardinality` field is returned so
      /// callers can see how large a result is once received; no server-side result
      /// limit is applied.
      /// </summary>
      /// <param name="request">The request to send to the server.</param>
      /// <param name="headers">The initial metadata to send with the call. This parameter is optional.</param>
      /// <param name="deadline">An optional deadline for the call. The call will be cancelled if deadline is hit.</param>
      /// <param name="cancellationToken">An optional token for canceling the call.</param>
      /// <returns>The call object.</returns>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public virtual grpc::AsyncUnaryCall<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse> ExperimentalRoaringLookupResourcesAsync(global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest request, grpc::Metadata headers = null, global::System.DateTime? deadline = null, global::System.Threading.CancellationToken cancellationToken = default(global::System.Threading.CancellationToken))
      {
        return ExperimentalRoaringLookupResourcesAsync(request, new grpc::CallOptions(headers, deadline, cancellationToken));
      }
      /// <summary>
      /// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
      /// of the resources of the given type on which the given subject has the
      /// given permission. This API is experimental and subject to change or
      /// removal.
      ///
      /// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
      /// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
      /// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
      /// query against a `long` field, once Base64-encoded.
      ///
      /// The IDs in the bitmap are the resource object IDs exactly as they appear
      /// in the relationships: no surrogate or internal ID is introduced, so the
      /// caller can use the bitmap directly against its own data (for example, a
      /// search index keyed by the same IDs).
      ///
      /// For this API to be usable, every resource object ID of the requested type
      /// must be a canonical decimal integer that fits in 44 bits -- at most
      /// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
      /// uint64 formatting unchanged: `document:007` and `document:7` are distinct
      /// objects that would collide as the integer 7, so non-canonical IDs are
      /// rejected. If any resource object ID violates either rule, the call fails
      /// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
      /// bitmap that is quietly too small is a wrong authorization answer. When the
      /// permission relates a type to itself (for example `group#member` looked up
      /// for a `group#member` subject), the subject is one of its own resources, so
      /// the subject's object ID is held to the same rules and can itself be the ID
      /// named in that error.
      ///
      /// Response size: the whole bitmap is returned in a single unary message, and
      /// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
      /// is compact -- a million sequential IDs encode in a few hundred bytes -- but
      /// sparse IDs cost close to 10 bytes each, so a result of more than roughly
      /// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
      /// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
      /// a limit of the caller's own gRPC configuration, not of the service: raise
      /// it to match the largest result you expect (in Go,
      /// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
      /// have an equivalent channel option). The `cardinality` field is returned so
      /// callers can see how large a result is once received; no server-side result
      /// limit is applied.
      /// </summary>
      /// <param name="request">The request to send to the server.</param>
      /// <param name="options">The options for the call.</param>
      /// <returns>The call object.</returns>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      public virtual grpc::AsyncUnaryCall<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse> ExperimentalRoaringLookupResourcesAsync(global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest request, grpc::CallOptions options)
      {
        return CallInvoker.AsyncUnaryCall(__Method_ExperimentalRoaringLookupResources, null, options, request);
      }
      /// <summary>Creates a new instance of client from given <c>ClientBaseConfiguration</c>.</summary>
      [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
      protected override RoaringLookupResourcesServiceClient NewInstance(ClientBaseConfiguration configuration)
      {
        return new RoaringLookupResourcesServiceClient(configuration);
      }
    }

    /// <summary>Creates service definition that can be registered with a server</summary>
    /// <param name="serviceImpl">An object implementing the server-side handling logic.</param>
    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    public static grpc::ServerServiceDefinition BindService(RoaringLookupResourcesServiceBase serviceImpl)
    {
      return grpc::ServerServiceDefinition.CreateBuilder()
          .AddMethod(__Method_ExperimentalRoaringLookupResources, serviceImpl.ExperimentalRoaringLookupResources).Build();
    }

    /// <summary>Register service method with a service binder with or without implementation. Useful when customizing the service binding logic.
    /// Note: this method is part of an experimental API that can change or be removed without any prior notice.</summary>
    /// <param name="serviceBinder">Service methods will be bound by calling <c>AddMethod</c> on this object.</param>
    /// <param name="serviceImpl">An object implementing the server-side handling logic.</param>
    [global::System.CodeDom.Compiler.GeneratedCode("grpc_csharp_plugin", null)]
    public static void BindService(grpc::ServiceBinderBase serviceBinder, RoaringLookupResourcesServiceBase serviceImpl)
    {
      serviceBinder.AddMethod(__Method_ExperimentalRoaringLookupResources, serviceImpl == null ? null : new grpc::UnaryServerMethod<global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesRequest, global::Authzed.Api.Materialize.V0.ExperimentalRoaringLookupResourcesResponse>(serviceImpl.ExperimentalRoaringLookupResources));
    }

  }
}
#endregion
